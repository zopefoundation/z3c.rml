language: python
sudo: false
before_install:
    - sudo apt-get update
# We need ghostscript at least 9.15 and japanese minch fonts
addons:
    apt:
        sources:
        - debian-sid
        packages:
        - ghostscript
        - fonts-takao-mincho
env:
    - TOXENV=py27
    - TOXENV=py33
    - TOXENV=py34
    - TOXENV=py35
install:
    - travis_retry pip install tox
script:
    - tox
notifications:
    email: false
